export interface PersonsType {
    id: string
    name: string
    isChecked: boolean
}

export interface GiftsType {
    id: string
    name: string
    pricePix: string
    priceCard: string
    url: string
    urlPayment: string
    quote: number
    isChecked: boolean
}